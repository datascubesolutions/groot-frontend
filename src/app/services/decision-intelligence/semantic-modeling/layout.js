export const metadata = {
  title: "Power BI Semantic Model Development | DAX Consulting | Groot Analytics",
  description: "Build certified Power BI semantic models with consistent DAX measures, row-level security, and Direct Lake performance on Microsoft Fabric. One source of truth.",
  keywords: "Power BI semantic model, DAX development, Power BI dataset, Direct Lake, row-level security Power BI, certified dataset"
};

/** @param {{ children: React.ReactNode }} props */
export default function Layout({ children }) {
  return <>{children}</>;
}
