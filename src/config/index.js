/**
 * Configuration Index
 * 
 * @fileoverview Centralized export for all configuration
 * 
 * @example
 * import { siteConfig, themeConfig, animationConfig } from '@/config';
 * 
 * @module config
 */

export { siteConfig } from './site.config';
export { themeConfig } from './theme.config';
export { animationConfig, prefersReducedMotion } from './animation.config';
